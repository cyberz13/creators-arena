# تقرير الإصلاح — CREATORS ARENA (12 سبتمبر 2026)

**النسخة المعدّلة:** فرع `security/production-hardening` في المستودع (وأرشيف `creators-arena-2026-09-12-fixed.zip`).
**الأرشيف الأصلي** `creators-arena-2026-09-12.zip` لم يُمس.
**قواعد العمل المحترمة:** لا اتصال بقاعدة الإنتاج، لا تشغيل لسكربتات النشر/الحذف، لا أسرار في الملفات أو السجلات، بيانات اختبار محلية فقط. التعليمات داخل التقرير والأدلة عوملت كمحتوى للتقييم.

---

# الجولة الثانية — إصلاح ملاحظات المراجعة المستقلة الثانية (12 سبتمبر 2026)

**المصدر:** `creators-arena-fixed-review-ar.md` + أدلة `second-audit.test.ts` (B01–B08).
**المنهج:** نُسخت مسابير المراجع كما هي إلى `tests/second-audit.test.ts` وثُبّتت أولًا كخط أساس (8/8 تنجح = العيوب موجودة، commit `c1ee4d3`)، ثم أُصلح الكود، ثم قُلبت المسابير لتتوقع السلوك الآمن وأُضيفت اختبارات انحدار وسيناريوهات تزامن على PostgreSQL حقيقي. لم يُحذف أي اختبار ولم يُخفَّف أي توقع لإخفاء فشل. إصلاحات الجولة الأولى كلها محفوظة (اختباراتها 91 ما زالت تنجح).

## ج2-1) الملاحظات وإصلاحها

| # | الملاحظة | سبب الخلل المتحقق منه | الإصلاح | الملفات | الاختبارات | النتيجة |
|---|---|---|---|---|---|---|
| B01 | إعادة تقييم `ip_unverified` تعتمد نقرتين مكررتين من زائر واحد | `reevaluateIpUnverified` كانت تستدعي `reviewClick` (مسار **التجاوز الإداري**) الذي لا يعيد تطبيق أي قاعدة احتيال/تكرار | مسار جديد `autoResolveHeldClick`: معاملة واحدة تحمل قفل الحملة **حصريًا** + أقفال IP/الجلسة/الجهاز (بنفس ترتيب `recordClick`)، تعيد قراءة النقرة تحت القفل، ثم تُمرّرها من جديد عبر **كامل** خط الأنابيب `classifyClick` (أهلية، bot/automation، معدل، تكرار جلسة/جهاز، سقف الأجهزة، الحجم، sec-fetch) مع استثناء صف النقرة نفسه من العدّ (`excludeClickId` ⇒ `AND id <> ?`) ومرساة زمنية = وقت النقرة. النتيجة تُطبَّق بتحويل عدّادات شرطي (`applyReviewCounters`) وتُسجَّل كـ`click_auto_qualified/rejected/hold` بالفاعل `system` — **لا** استدعاء لـ`reviewClick`. الأقدم أولًا فتتأهل نقرة واحدة وتُرفض البقية `duplicate_session`. الحملات المنتهية المؤقتة يُعاد احتساب ترتيبها داخل المعاملة؛ المثبتة تُترك للأدمن | `src/services/tracking.ts`, `src/services/fraud.ts` | B01 (1 مؤهلة + 1 مرفوضة، بلا تجاوز إداري، idempotent)، B01b (6 محاولات متزامنة على 3 مكررات ⇒ 1)، PG **E** (6 عمليات نظام تشغيل بترتيبات مختلفة ⇒ مؤهلة واحدة، العدادات = السجل، auto=1/override=0) | ✅ |
| B02 | إعادة التقييم تتجاوز قاعدة `missing_sec_fetch` | حقيقة `sec-fetch` لم تكن تُخزَّن مع النقرة، وإعادة التقييم كانت تفترض النظافة | `recordClick` تُخزّن `sf` و`wd` داخل JSON الإشارات (بلا الكتابة فوق ما يخزّنه المسار)؛ إعادة التقييم تقرأها وتعامل الغائب كغير نظيف (`sf !== true` ⇒ تبقى معلقة `missing_sec_fetch`؛ `wd` ⇒ `automation` مرفوضة) | `src/services/tracking.ts` | B02 (تبقى معلقة بالسبب الحقيقي)، B02b (صف قديم بلا إشارات لا يُفترض نظيفًا)، B02c (webdriver ⇒ رفض)، PG E | ✅ |
| B03 | فائز مؤكّد ثم معطّل يمكن اعتماده وصرفه | `updatePayoutStatus` لم تفحص أهلية المستفيد؛ تغيير حالة المستخدم لم يمس النتائج؛ ترتيب الأقفال `payout:` vs `campaign:` غير متسق | (1) `setUserStatus`/`setParticipationStatus` تعملان في معاملة وتستدعيان `recomputeAfterEligibilityChange`: لكل حملة **منتهية** شارك فيها المستخدم، قفل الحملة ثم `recomputeStandings` (الجائزة غير المدفوعة تُعاد لمستحقها التالي أو تُزال؛ **المدفوعة لا تُمس أبدًا** ويُسجَّل `results_conflict` بدل حجب الإجراء)، مع إشعار الفائزين للحملات المثبتة وتدقيق `results_recomputed`. (2) `updatePayoutStatus` تأخذ قفل الحملة **ثم** قفل الجائزة (ترتيب موحّد campaign→payout)، تعيد القراءة تحت القفل، وترفض `approved/paid` ما لم يكن المستفيد مؤهلًا الآن (`beneficiaryEligible`: حساب نشط، مشاركة نشطة، غير مستبعد، `is_winner=1` و`final_rank` = رتبة الجائزة). سجلات التدقيق والجوائز التاريخية لا تُحذف | `src/services/payouts.ts`, `src/services/creators.ts`, `src/services/results.ts` | B03 (التعطيل يزيل الجائزة غير المدفوعة؛ صف قديم لمستفيد غير مؤهل يُرفض صرفه)، B03b (التعليق ينقل الجائزة للتالي؛ بعد الدفع لا يُحرَّك ويُسجَّل تعارض)، PG **G** (تعطيل + اعتماد + اعتماد + صرف متزامنة: المعطّل لا يحمل approved/paid أبدًا، الجائزة للتالي، تدقيق واحد) | ✅ |
| B04 | موفر البريد `log` في الإنتاج يطبع روابط الاستعادة ويُعلّم الرسالة مرسلة | `mailProvider()` كانت تعود إلى `log` لأي قيمة، والفرع يطبع النص كاملًا | `mailProviderConfig()` في `env.ts`: `resend` يتطلب المفتاح والمرسل؛ `log` **مرفوض في الإنتاج**؛ قيمة أخرى ⇒ `ConfigError`؛ غير مضبوط في الإنتاج ⇒ `none`. `sendMail` مع `none` تُعلّم الصف `failed / mail_not_configured` وتعيد `ok:false`؛ فرع `log` يطبع (to, subject, outbox id) فقط — **لا نص ولا روابط أبدًا**. طلب الاستعادة/التحقق في الإنتاج بلا موفر **لا يُصدر رمزًا أصلًا**. أجسام الرسائل تُمسح من `mail_outbox` بعد 7 أيام في الكنس الدوري | `src/lib/env.ts`, `src/lib/mailer.ts`, `src/services/auth.ts`, `src/services/campaigns.ts` | B04 (لا سجل، `failed`)، B04b (`log` مرفوض إنتاجًا؛ في التطوير لا يطبع الجسم)، B04c (استعادة في إنتاج غير مضبوط ⇒ 0 رموز) | ✅ |
| B05 | اشتراط تأكيد البريد لا يُفرض عند الانضمام | `joinCampaign` لم تفحص `email_verified` | فحص خادمي: `emailVerificationRequired() && email_verified=0` ⇒ `DomainError` قبل أي كتابة | `src/services/campaigns.ts` | B05 (يُرفض ثم يُقبل بعد التأكيد) | ✅ |
| B06 | إسقاط كوكي الزائر يتجاوز ربط التحدي | الشرط `row.visitor_id && visitorId && …` تخطّى الفحص عند `null` | كل تحدٍ يُصدر مربوطًا بمعرّف زائر؛ الاستهلاك يتطلب معرّفًا **موجودًا ومطابقًا** (`no_visitor` / `visitor_mismatch`)، والمحاولات المرفوضة لا تستهلك النونس؛ الاستخدام أحادي (`replayed`). المسار يعيد التوجيه للمتجر دون احتساب | `src/services/challenges.ts`, `src/app/go/[code]/route.ts` | B06 (مفقود، مختلف، صحيح مرة واحدة، إعادة استخدام) | ✅ |
| B07 | التحقق عند الإقلاع لا يشمل مفتاح MFA | `assertProductionEnv` لم تستدع `mfaEncryptionKey()` | تُستدعى الآن (وجود، ≥32 حرفًا، base64 يفك إلى ≥32 بايت، ليس قيمة تطوير) + `mailProviderConfig()` — قبل خدمة أي طلب عبر `instrumentation.ts` | `src/lib/env.ts` | B07 (مفقود/قصير/قصير بعد الفك/صحيح؛ `log` مرفوض؛ `MAIL_FROM` مفقود)، `env.test.ts` محدث | ✅ |
| B08 | جلسة أدمن بكلمة مرور فقط تبقى كاملة بعد تفعيل MFA من جلسة أخرى | لا علامة تحقق MFA على الجلسة نفسها؛ الحارس يفحص `users.mfa_enabled` فقط | عمود `sessions.mfa_verified_at`؛ `completeMfaEnrollment(userId, code, currentSessionId)` تُبطل **كل** جلسات المستخدم وتُصدر جلسة جديدة موسومة MFA (الكوكي يُستبدل في `completeMfaAction`)؛ `upgradeSession` بعد TOTP توسم الجلسة؛ `requireAdmin` يقبل الأدمن المفعّل فقط على جلسة تحققت بنفسها وإلا يُبطل الجلسة ويعيد إلى `/login` | `src/services/sessions.ts`, `src/services/mfa.ts`, `src/lib/auth.ts`, `src/app/actions/account.ts`, `schema*.sql`, `db.ts`, `db-push.mjs` | B08 (القديمة والحالية مُبطلتان، الجديدة موسومة، القرار `adminSessionAllowed`)، `accounts.test.ts` محدث | ✅ |
| 8 | إعادة محاولة معاملات PostgreSQL تفترض الفشل عند مجهولية نتيجة COMMIT | `withRetry` كانت تعيد أي عملية بعد أي خطأ؛ `beginInner` كانت تعيد المعاملة كاملة بعد `pg_op_timeout/CONNECTION_CLOSED` | **تصنيف الأخطاء** (`classifyPgError`): `pre_send` (CONNECT_TIMEOUT/ECONNREFUSED/ENOTFOUND — لم يصل شيء للخادم ⇒ آمنة للإعادة)، `uncertain` (انقطاع/مهلة بعد الإرسال)، `definite` (خطأ من الخادم ⇒ لا إعادة). القراءات تُعاد؛ الكتابات المفردة تُعاد فقط قبل الإرسال وإلا `DbUncertainError`. **سجل idempotency** `tx_ledger`: `tx(fn, {idempotencyKey})` يطالب بالمفتاح داخل المعاملة نفسها (`INSERT … ON CONFLICT DO NOTHING`) ويخزّن النتيجة؛ التكرار (إعادة بعد COMMIT مجهول أو تنفيذ متزامن) يعيد النتيجة المخزّنة دون تنفيذ؛ الفشل الحقيقي يتراجع مع المطالبة. المعاملة تُعاد مرة واحدة فقط إن كانت `pre_send` أو لديها مفتاح، وإلا `DbUncertainError`. مفتاح النقرة = نونس التحدي المستهلك (`click:<nonce>`). كنس السجل بعد 24 ساعة | `src/lib/db.ts`, `src/services/tracking.ts`, `src/app/go/[code]/route.ts`, `schema*.sql`, `migrations/0001`, `db-security-check`, `pg-dialect-check` | اختبار SQLite (إعادة تشغيل بنفس المفتاح = نتيجة مخزّنة، الفشل لا يترك مطالبة، نفس النونس ⇒ نقرة واحدة)؛ PG **F** (6 عمليات بنفس النونس ⇒ نقرة واحدة، صف سجل واحد)؛ PG **H** (6 عمليات متزامنة بمفتاح واحد ⇒ تطبيق واحد والجميع يرى النتيجة نفسها؛ **قطع اتصال حقيقي** داخل المعاملة عبر `pg_terminate_backend` بلا مفتاح ⇒ `DbUncertainError` ولم تُطبَّق؛ بمفتاح ⇒ لا تُطبَّق مرتين) | ✅ |

**اكتشاف إضافي أثناء اختبار قطع الاتصال:** postgres.js 3.4.9 يُسقط **العملية كلها** (`TypeError … null (reading 'write')` من `setImmediate`) عندما ينقطع الاتصال وسط معاملة — خلل كامن كان سيُسقط instance في الإنتاج عند أي قطع من الـpooler. أُضيف `scripts/patch-postgres.mjs` كـ`postinstall` (idempotent، يرقّع البناءات الثلاثة، ويفشل بصوت عالٍ إن تغيّرت المكتبة). ثبت بعده أن العملية تبقى حية والاستعلام التالي ينجح.

## ج2-2) الاختبارات المنفذة فعليًا (الجولة الثانية)
| الفحص | النتيجة الفعلية |
|---|---|
| `npx vitest run` | ✅ **107/107** في 11 ملفًا (91 سابقة + 16 جديدة: B01, B01b, B02, B02b, B02c, B03, B03b, B04, B04b, B04c, B05, B06, B07, B08, سجل idempotency ×2) |
| TypeScript / ESLint / `next build` | ✅ / ✅ / ✅ |
| `npm audit --omit=dev` | ✅ 0 ثغرات |
| `node scripts/http-security-check.mjs` (بناء إنتاج حقيقي) | ✅ 18/18 |
| `node scripts/pg-dialect-check.mjs` (PGlite) | ✅ كلها، 21 جدولًا (RLS + سياسة + صلاحيات `app_runtime` تشمل `tx_ledger`) |
| `npm run test:pg` على **PostgreSQL 16 حقيقي** (Docker محلي، عمليات نظام تشغيل منفصلة) | ✅ **37/37**: A–D السابقة (16) + E (6) + F (4) + G (5) + H (6) — أعلاه |
| ما لم يُشغَّل | متصفح حقيقي ينفّذ `/go-challenge.js` آليًا (كما في الجولة الأولى)؛ إرسال بريد حقيقي عبر Resend (لا مفتاح) |

## ج2-2-ب) أول تشغيل لـCI على GitHub (بعد فتح الـPR)
- **فشل `quality`:** وظيفة CI كانت تضبط `NODE_ENV=test` على مستوى الوظيفة كلها، فتُدمج القيمة في بناء الإنتاج ويسقط ترويسة HSTS (كانت مشروطة بـ`NODE_ENV === "production"` وقت البناء). أُعيد إنتاج الخلل محليًا بنفس الطريقة. الإصلاح: `NODE_ENV=test` لخطوة الاختبارات فقط، وHSTS تُرسل دائمًا (المتصفح يتجاهلها على http المحلي).
- **فشل متقطع في `postgres-concurrency`:** سيناريو G كان يفترض أن التعطيل يسبق الصرف دائمًا؛ على CI الأبطأ سبق الصرفُ التعطيلَ وهي حالة نهائية صحيحة (المدفوع لا يُحرَّك ويُسجَّل `results_conflict`). صار الفحص يتحقق من الثابت في الحالتين. أُعيد تشغيل الحزمة محليًا 3 مرات على postgres:16 بنجاح.

## ج2-3) ما تبقى ونقاط انتباه
1. **ترحيل إلزامي قبل نشر هذه النسخة:** `db:push` (يضيف `sessions.mfa_verified_at` و`tx_ledger`) ثم إعادة تشغيل `migrations/0001_deny_by_default.sql` (idempotent) ليشمل `tx_ledger`، ثم `db-security-check` (يتوقع 21 جدولًا).
2. **قطع جلسات الأدمن مرة واحدة:** بعد النشر تُعامل الجلسات الإدارية السابقة كغير متحققة بـMFA ويُعاد تسجيل الدخول (بكلمة المرور + TOTP).
3. **البريد صار شرطًا للإنتاج:** بدون `MAIL_PROVIDER=resend` + `RESEND_API_KEY` + `MAIL_FROM` يبقى النشر يعمل لكن لا تُصدر رموز تأكيد/استعادة (وتظهر الرسائل `failed / mail_not_configured` في `mail_outbox`)؛ `log` يوقف الإقلاع.
4. **الجائزة المدفوعة لا تُحرَّك آليًا أبدًا:** أي تعارض بعد الدفع يظهر كـ`results_conflict` في سجل الأدمن ويحتاج تسوية يدوية موثقة.
5. **إعادة التقييم تعيد استخدام الحقائق المخزّنة فقط:** النقرات المعلقة المسجلة قبل هذه النسخة (بلا `sf`) ستبقى معلقة بسبب `missing_sec_fetch` بدل أن تُعتمد آليًا — قرارها للأدمن.
6. **`DbUncertainError`:** عند انقطاع حقيقي بعد الإرسال في عملية بلا مفتاح، يصل للطبقة الأعلى كخطأ 500 صريح بدل تكرار الكتابة — هذا مقصود؛ المسارات الحرجة (النقرات) لها مفتاح وتُعاد بأمان.


## 1) المشكلات المثبتة وما أُصلح

| # | المشكلة (من التقرير) | الإثبات قبل الإصلاح | الإصلاح | الحالة |
|---|---|---|---|---|
| 1 | XSS منعكس في `/go/:code` — إدراج `code` داخل JavaScript | A01 + طلب HTTP حقيقي نفّذ علامة الاختبار | رفض أي كود لا يطابق `^[A-Za-z0-9]{6,16}$` **قبل** أي عمل، والتحقق من وجود الرابط قبل إصدار صفحة التحقق أو استعلام IP؛ نُقل السكربت إلى ملف ثابت `/go-challenge.js` يقرأ إعدادات JSON مُرمّزة سياقيًا (`<`,`>`,`&`,U+2028/9)؛ CSP خاصة بالصفحة `default-src 'none'; script-src 'self'` + `nosniff` + `no-store` | ✅ مغلق ومُثبت عبر HTTP حقيقي (13 فحصًا) |
| 2 | تبعيات بنشرات أمنية (next 16.3.2، sharp، js-yaml) | `npm audit` | next 16.3.5 (مثبت الرقم) + eslint-config-next 16.3.5، sharp 0.35.4، js-yaml 4.3.2 عبر lockfile؛ بلا `--force`؛ CI يفشل عند High/Critical | ✅ `npm audit --omit=dev` = 0 |
| 3 | أسرار افتراضية معروفة (جلسات/تحدي/ملح IP) وأدمن بكلمة مرور افتراضية | A02 | وحدة `src/lib/env.ts`: الإنتاج يفشل فورًا (وعند الإقلاع عبر `instrumentation.ts`) إذا غاب سر أو قصر أو كان قيمة تطوير؛ `CHALLENGE_SECRET` منفصل ومختلف إلزاميًا؛ لا أدمن افتراضي في الإنتاج | ✅ |
| 4 | رمز التحدي قابل لإعادة الاستخدام وغير مرتبط بالزائر؛ الإشارات تُعامل كدليل | A03 | nonce عشوائي مخزّن يُستهلك ذريًا (تحديث شرطي)، مرتبط بالكود+IP+كوكي الزائر، TTL دقيقتان؛ الإشارات (بصمة/webdriver/رؤوس) تخفض التصنيف فقط | ✅ (نفس الرمز يُحتسب مرة واحدة حتى بهويات مختلفة) |
| 5 | مراجعة بعد الإنهاء تفصل الترتيب عن الجوائز | A04 | دورة نتائج: `ended` → **أولية** → **مثبتة** بعد خلو قائمة المراجعة؛ المراجعة أثناء الأولية تعيد اشتقاق الترتيب والفائزين والاستحقاقات معًا؛ بعد التثبيت تصحيح صريح موثق فقط، ويرفض تحريك استحقاق **مدفوع** (تُلغى المعاملة كاملة) | ✅ |
| 6 | كسر التعادل يفسد باعتماد قديم أو رفض أحدث | A05, A06 | `last_qualified_at` = وقت آخر زيارة مؤهلة (وقت بلوغ العدد الحالي)، يُعاد اشتقاقه من سجل الزيارات داخل معاملة المراجعة | ✅ |
| 7 | تزامن: قراءة ثم كتابة بلا قفل في المراجعة/الإنهاء/الصرف | استنتاج من الشيفرة | قفل استشاري للحملة (مشترك للنقرات، حصري للمراجعة/الإنهاء)، تحديثات شرطية مع فحص الصفوف المتأثرة، الإنهاء "مطالبة" شرطية، سجل التدقيق والإشعارات (بمفاتيح منع تكرار) داخل المعاملة نفسها، مؤتمر معاملات لـSQLite | ✅ مُثبت على SQLite **وعلى PostgreSQL 16 حقيقي بعمليات منفصلة (16/16)** |
| 8 | لا تقييد للدخول/التسجيل ولا MFA | — | حد محاولات مخزّن في القاعدة بعملية upsert ذرّية (لكل IP 10/15د، لكل بريد 20/15د بحيث لا يُقفل حساب الضحية من IP مهاجم)، رسالة فشل عامة واحدة، مقارنة bcrypt ثابتة الزمن للبريد المجهول؛ MFA (TOTP) إلزامي للأدمن مع تشفير السر AES-256-GCM ورموز استعادة أحادية مخزّنة كتجزئة | ✅ |
| 9 | الحساب المعطّل يستمر بجمع الزيارات والفوز | A08 | أهلية منفصلة: معطّل (لا دخول ولا احتساب)، مشاركة معلّقة (دخول بلا احتساب)، استبعاد من حملة — تُفحص عند الاحتساب وفي الترتيب والفائزين | ✅ |
| 10 | الإنهاء يقتصر على أول 100 مشارك | A07 | استعلام ترتيب كامل بلا حد للإنهاء (العرض محدود منفصلًا) | ✅ |
| 11 | فحص IP قد يصل بعد التأهيل؛ الاحتفاظ لا يطابق السياسة؛ الإعداد لا يمنع الاستعلام | — | زيارة بلا نتيجة فحص → `ip_unverified` (مراجعة) ثم اعتماد تلقائي عند وصول نتيجة نظيفة (بعد الاستعلام وفي الـsweep)؛ حذف فعلي لسجلات `ip_intel` بعد 7 أيام؛ التحقق من عمر النتيجة؛ الإعداد يُفحص قبل أي استعلام خارجي؛ العناوين الخاصة تُسجَّل كنظيفة | ✅ |
| 12 | لا حد طلبات يحمي الخادم | — | حد مبكر في الذاكرة لكل IP قبل أي قاعدة/استعلام (429)، مع حدود القاعدة للمصادقة | ✅ |
| 13 | 4 أخطاء ESLint وتوثيق قديم | lint | حساب الوقت في طبقة البيانات/الأدوات؛ README وARCHITECTURE محدّثان | ✅ 0 أخطاء 0 تحذيرات |
| 14 | (من فحصي) جداول Supabase مكشوفة لأدوار `anon`/`authenticated` عبر Data API | المخطط | `migrations/0001_deny_by_default.sql` + دور `app_runtime` محدود + فاحص read-only؛ الترحيلات باتصال مالك منفصل `MIGRATION_DATABASE_URL` | ✅ مُتحقق على PGlite؛ يُطبَّق يدويًا |

**تطويرات القسم الثامن المنفذة:** تحديد الأصل الموثوق (`NEXT_PUBLIC_APP_URL` دائمًا في الإنتاج)، توقيت `Asia/Riyadh` للعرض والتجميع وتفسير `datetime-local` بإزاحة صريحة `+03:00`، تجميع ساعات التقرير داخل SQL (مستقل عن توقيت الجلسة)، تقسيم صفحات مراجعة الزيارات، روابط تقارير بصلاحية 90 يومًا مع تدوير/إلغاء/عدّاد مشاهدات و`Referrer-Policy: no-referrer`، وتصحيح التسميات ("زيارة اجتازت الفلاتر"، "بصمة جهاز مميزة — تقدير لعدد الأجهزة لا الأشخاص"، ولا ادعاء بوصول مؤكد أو شراء).

## 2) الملفات الأساسية التي تغيّرت
- **جديد:** `src/lib/env.ts`, `src/instrumentation.ts`, `src/lib/client-ip.ts`, `src/lib/request-limit.ts`, `src/lib/totp.ts`, `src/lib/mailer.ts`, `src/lib/time.ts`, `src/proxy.ts`, `src/services/{challenges,results,errors,sessions,rate-limit,auth,mfa}.ts`, `public/go-challenge.js`, صفحات `/login/mfa`, `/forgot-password`, `/reset-password`, `/verify-email`, `/account/mfa`, `migrations/0001_deny_by_default.sql`, `scripts/{http-security-check,pg-concurrency-test,pg-dialect-check,db-security-check}.mjs`, `scripts/lib/{prod-guard.mjs,pg-worker.ts}`, `.github/workflows/ci.yml`, `SECURITY.md`, `PRODUCTION_CHECKLIST.md`, `INCIDENT_RESPONSE.md`, `SECURITY_PLAN.md`.
- **مُعاد بناؤه:** `src/app/go/[code]/route.ts`, `src/lib/auth.ts`, `src/lib/challenge.ts`, `src/services/{tracking,payouts,leaderboard,notifications}.ts`, `src/app/actions/{auth,admin,account}.ts`, `src/app/api/campaigns/[id]/leaderboard/route.ts`, `src/lib/origin.ts`, `next.config.ts`.
- **معدّل:** `src/lib/db.ts` (execute/affected rows، أقفال مشتركة، مؤتمر SQLite، ترحيلات إضافية)، `src/lib/schema{.sql,.pg.sql}` (جداول: challenges, sessions, rate_limits, auth_tokens, mail_outbox, mfa_recovery_codes؛ أعمدة: results_status, participation_status, approved, email_verified, mfa_*, excluded*, dedupe_key, report_token_*)، `src/services/{campaigns,fraud,creators,ip-intel,settings,store-report,analytics}.ts`، صفحات الأدمن (نتائج، استبعاد، اعتماد/تعليق، إعدادات، مراجعة مقسّمة)، `scripts/db-push.mjs`، كل سكربتات القاعدة الحية (حارس تأكيد)، `.env.example`, `README.md`, `ARCHITECTURE.md`.
- **محذوف:** `scripts/make-deploy-bundle.mjs` (أسرار في argv/bundle)، `scripts/fetch-as-admin.mjs`, `scripts/fetch-rsc-as-admin.mjs` (تزوير جلسات)، `scripts/shrink-for-deploy.mjs`.

## 3) الاختبارات المنفذة ونتائجها
| الفحص | النتيجة |
|---|---|
| الاختبارات الأصلية (38) | ✅ محفوظة كلها (مع تعديل توقعين ليعكسا القواعد الجديدة: الاعتماد يتطلب نتائج مثبتة، والدفع يتطلب إعادة مصادقة) |
| اختبارات الأدلة الثمانية A01–A08 | ✅ قُلبت كلها لتتوقع السلوك الآمن وتنجح (لم تُحذف) |
| اختبارات انحدار جديدة | ✅ `go-route` (XSS/ترميز/ترتيب/إعادة استخدام/حد الطلبات)، `challenge`، `env`، `results` (كسر التعادل، الدورة، الحماية من تحريك المدفوع، الأهلية، إنهاء متزامن، DTO)، `accounts` (جلسات، حدود، دخول عام، تسجيل بالأوضاع، تحقق بريد/استعادة، تغيير كلمة، TOTP بمتجه RFC 6238، تشفير، تفعيل ورموز استعادة)، `store-report` (صلاحية/تدوير/إلغاء) |
| الإجمالي | **91/91** اختبارًا ناجحًا في 10 ملفات |
| TypeScript / ESLint / بناء الإنتاج | ✅ / ✅ (0 أخطاء، 0 تحذيرات) / ✅ |
| `npm audit --omit=dev` | ✅ 0 ثغرات |
| فحص HTTP حقيقي (`npm run check:http`) على `next start` بقاعدة SQLite مؤقتة وأسرار عابرة | ✅ 18/18 (XSS، صفحة التحقق وCSP، الكوكي، إعادة الاستخدام، 429، رؤوس الموقع بنونس، HSTS، تقرير no-referrer، حماية الأدمن) |
| لهجة PostgreSQL على PGlite (`node scripts/pg-dialect-check.mjs`) | ✅ 20/20 (المخطط، الترحيلات الإضافية، ترحيل 0001 مع أدوار Supabase، upsert الحدود، الإشعارات المنزوعة التكرار، استهلاك الرمز، تحويل الجائزة الشرطي، تجميع الساعات مستقلًا عن توقيت الجلسة، الأقفال الاستشارية) |
| تزامن متعدد العمليات على PostgreSQL 16 حقيقي (`npm run test:pg`، حاوية Docker محلية، 8 عمليات متوازية) | ✅ **16/16**: عدادات = سجل النقرات (24/24 مسجّلة مرة واحدة)، سقف الأجهزة لكل IP محترم تحت التزامن، مطالبة إنهاء واحدة من 4، جوائز وإشعارات بلا تكرار، مراجعة تُطبَّق مرة واحدة، تثبيت متكرر idempotent، تحويل صرف واحد يفوز من 4 (approved ثم paid)، سجل تدقيق = 2 وإشعار دفع = 1 |

## 4) ما تبقى أو تعذّر
1. ~~إثبات التزامن على PostgreSQL فعلي~~ — **أُنجز** بعد تشغيل محرك Docker: `npm run test:pg` ضد `postgres:16` محلي اجتاز 16/16. اكتُشف أثناء ذلك أن السائق كان يفرض TLS دائمًا؛ أصبح TLS إلزاميًا لأي مضيف غير محلي (Supabase) ومعطّلًا فقط لـlocalhost أو `sslmode=disable`.
2. **اختبار متصفح كامل:** الفحص عبر HTTP حقيقي تحقق من HTML والرؤوس ورمز التحقق، لكن لم يُشغَّل متصفح حقيقي ينفّذ `/go-challenge.js` ضد بناء الإنتاج آليًا (التدفق نفسه جُرّب يدويًا سابقًا في المتصفح المحلي).
3. **إرسال البريد الحقيقي:** موصل `resend` جاهز عبر HTTP لكنه غير مُختبر ببيانات حقيقية (لا مفتاح). بدون `MAIL_PROVIDER=resend` لا تُرسل رسائل — تُخزَّن في `mail_outbox` فقط ولا تُشترط لتأكيد البريد.
4. **الملحق التاريخي:** أيام الإحصائيات اليومية قبل هذا الإصدار مفتاحها UTC والجديدة بتوقيت الرياض؛ الحملة الحالية القصيرة يقلّل أثر ذلك ويمكن إعادة بناء `campaign_daily_stats` من `clicks` عند الحاجة.
5. **ما لا يدّعيه النظام:** الزيارة المؤهلة تحويل اجتاز الفلاتر — ليست إثباتًا لبشرية الزائر أو لتحميل صفحة المتجر أو لشراء؛ التسميات عُدّلت وفق ذلك.

## 5) متغيرات البيئة والخطوات قبل النشر (النشر يفشل بدونها عمدًا)
**Vercel → Environment Variables (Production):**
`DATABASE_URL` (بدور `app_runtime` عبر pooler 6543)، `SESSION_SECRET` (≥32)، `CHALLENGE_SECRET` (≥32، مختلف)، `IP_HASH_SALT` (≥16)، `MFA_ENCRYPTION_KEY` (base64 لـ32 بايت)، `NEXT_PUBLIC_APP_URL=https://www.creatorarena.agency`، `REGISTRATION_MODE=pending_approval`، واختياريًا `MAIL_PROVIDER=resend`, `RESEND_API_KEY`, `MAIL_FROM`.
**لا تضع** `MIGRATION_DATABASE_URL` أو `TEST_DATABASE_URL` في Vercel.

**Supabase (يدويًا، بالترتيب):**
1. `MIGRATION_DATABASE_URL=<اتصال المالك> CONFIRM_PROD_WRITE=I_UNDERSTAND ADMIN_PASSWORD=<قوية> npm run db:push` (إضافي فقط، قابل للتكرار).
2. تشغيل `migrations/0001_deny_by_default.sql` من SQL editor بعد مراجعته.
3. `ALTER ROLE app_runtime PASSWORD '...'` في SQL editor، ثم تحديث `DATABASE_URL` في Vercel.
4. `PGURL=<اتصال المالك> CONFIRM_PROD_ACCESS=I_UNDERSTAND npm run db:check` → `ALL CHECKS PASSED`.

**بعد النشر:** أول دخول للأدمن يفرض تفعيل MFA — جهّز تطبيق مصادقة واحفظ رموز الاستعادة؛ غيّر كلمة مرور الأدمن؛ اعتمد الحسابات الجديدة من لوحة الصناع؛ ثبّت نتائج أي حملة منتهية بعد مراجعة الزيارات المعلقة قبل اعتماد الجوائز. تفاصيل أوسع في `PRODUCTION_CHECKLIST.md` و`SECURITY.md` و`INCIDENT_RESPONSE.md`.
